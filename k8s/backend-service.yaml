apiVersion: v1
kind: Service
metadata:
  name: traceback-backend-service

spec:
  selector:
    app: traceback-backend

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081

  type: NodePort